# Claude Authentication (choose one)

# Option 1: Use Claude credentials file/keychain (recommended)
# Set to "true" to use ~/.claude/.credentials.json or macOS keychain
USE_CLAUDE_CREDENTIALS=true

# Option 2: API key fallback (if credentials not available)
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# Optional: Repository to clone on startup
# REPO_URL=https://github.com/username/repo.git

# Optional: GitHub Personal Access Token
# Required for private repos and push access
# Create at: https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Optional: Git user configuration (auto-detected from host)
# GIT_USER_NAME=Your Name
# GIT_USER_EMAIL=your.email@example.com